<?php
/**
 * 9-dot app launcher (suite: Orangescrum / Wiki / Reports).
 *
 * Open-source edition ships only Orangescrum, so the cross-app launcher is
 * removed. Emitting no output makes header_inner.php skip the launcher <li>
 * (it guards with `if (trim($appLauncherHtml) !== '')`).
 */
return;
